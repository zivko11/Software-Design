package view;

import org.fife.ui.rsyntaxtextarea.RSyntaxDocument;
import org.fife.ui.rsyntaxtextarea.RSyntaxTextArea;

/**
 * Klasa <class>TekstEditor</class> omogucava oznacavanje teksta u skladu sa
 * sintaksom odredjenog programskog jezika. Nasledjuje
 * <code>RSyntaxTextArea</code> klasu.
 * 
 * @author Nikola Stojanovic
 */

@SuppressWarnings("serial")
public class TekstEditor extends RSyntaxTextArea {

	public TekstEditor() {
		super();
		// TODO Auto-generated constructor stub
	}

/**
* Kreira tekst editor sa odredjenim brojem redova i kolona
*
* @param rows
*            broj redova editora
* @param cols
*            broj kolona
*/
	public TekstEditor(int rows, int cols) {
		super(rows, cols);
	}

	public TekstEditor(int textMode) {
		super(textMode);
	}

	public TekstEditor(RSyntaxDocument doc, String text, int rows, int cols) {
		super(doc, text, rows, cols);
	}

	public TekstEditor(RSyntaxDocument doc) {
		super(doc);
	}

	public TekstEditor(String text, int rows, int cols) {
		super(text, rows, cols);
		// TODO Auto-generated constructor stub
	}

}